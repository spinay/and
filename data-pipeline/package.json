{
  "name": "ipo-keeper-data-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "공모주 알리미 - 데이터 파이프라인 (CSV → JSON 빌드)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs data/ipos.json",
    "all": "npm run build && npm run validate"
  },
  "dependencies": {
    "csv-parse": "^5.5.6"
  },
  "engines": {
    "node": ">=20"
  }
}
